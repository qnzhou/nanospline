name: build and test

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: install Eigen
      run: |
          apt-get update
          apt-get install -y libeigen3-dev
    - name: build and run
      run: |
        mkdir build
        cd build
        cmake ..
        make
        ./nanospline_test
